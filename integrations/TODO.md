# TODO

- Revisit the Lob HTML template overflow behavior before generating longer or dynamic studies. The current template uses fixed page regions with `overflow:hidden`, so longer content can be clipped if the HTML is expanded beyond the tested first-study case.
